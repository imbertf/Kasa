import React from "react";
import logo from "../assets/images/logo.svg";

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} alt="logo de Kasa" />
        </div>
    );
};

export default Logo;
